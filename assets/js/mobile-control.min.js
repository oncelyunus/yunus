// Get URL Parameters
function GetURLParameter(sParam) {
    var sPageURL = window.location.search.substring(1);
    var sURLVariables = sPageURL.split('&');
    for (var i = 0; i < sURLVariables.length; i++) {
        var sParameterName = sURLVariables[i].split('=');
        if (sParameterName[0] === sParam) {
            return sParameterName[1];
        }
    }
}

// Mobile Devices Checker
var isMobile = {
    Windows: function () {
        return /IEMobile/i.test(navigator.userAgent);
    },
    Android: function () {
        return /Android/i.test(navigator.userAgent);
    },
    BlackBerry: function () {
        return /BlackBerry/i.test(navigator.userAgent);
    },
    iOS: function () {
        return /iPhone|iPad|iPod/i.test(navigator.userAgent);
    },
    iPhone: function () {
        return /iPhone|iPod/i.test(navigator.userAgent);
    },
    iPad: function () {
        return /iPad/i.test(navigator.userAgent);
    },
    any: function () {
        return (isMobile.Android() || isMobile.BlackBerry() || isMobile.iOS() || isMobile.Windows());
    }
};

var isOpera = (!!window.opr && !!opr.addons) || !!window.opera || navigator.userAgent.indexOf(' OPR/') >= 0;

// Get Android Function
function getAndroidVersion() {
    var v = parseFloat(navigator.userAgent.match(/Android\s+([\d\.]+)/)[1]);
    return v;
}
$(document).ready(function () {

    // Get ShowIn Parameters
    var showin = GetURLParameter('showin');

    // Body Class Function
    if (isMobile.any()) {
        $('body').addClass('isMobile');
    }
    if (isMobile.iOS()) {
        $('body').addClass('iOS');
    }
    if (isMobile.BlackBerry()) {
        $('body').addClass('BlackBerry');
    }
    if (isMobile.Android()) {
        $('body').addClass('Android');
    }
    if (isMobile.Windows()) {
        $('body').addClass('Windows');
    }
    if (isMobile.iPhone()) {
        $('body').addClass('iPhone');
    }
    if (isMobile.iPad()) {
        $('body').addClass('iPad');
    }
    if (showin === 'app') {
        $('body').addClass('app');
    }

    // Social Share Whatsapp Hide for Desktop
    if (showin === 'app') {
        // $('.app-urls').remove();
    }

    if ($('body').hasClass('iOS')) {
        $('.app-urls,' + '.popup-content__button_android').remove();
    } else if ($('body').hasClass('Android')) {
        $('.app-urls,' + '.popup-content__button_apple').remove();
    }

});